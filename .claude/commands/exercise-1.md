---
description: Walk the user through Exercise 1 — sub-agents in parallel.
---

The user is starting Exercise 1 of the workshop.

Read `EXERCISES.md` and walk them through Exercise 1 step by step. Don't run the agents for them — they should do that themselves so they see the boxes spin up. Your job is to:

1. Confirm the project is set up (`uv sync` ran clean, `pytest` is green).
2. Point them at the right prompt to paste in.
3. Let them paste it.
4. After both sub-agents return, ask them what they noticed about parallelism, isolation, and tool restrictions.

Keep the dialogue tight. They're learning by doing, not by reading.
