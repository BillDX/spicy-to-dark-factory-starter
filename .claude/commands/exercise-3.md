---
description: Walk the user through Exercise 3 — agent teams in tmux.
---

The user is starting Exercise 3.

Read `EXERCISES.md` Exercise 3. Walk them through:

1. Confirming `tmux` is installed.
2. Setting up three worktrees — one per pane.
3. Launching Claude in each pane.
4. Giving the lead a task that requires negotiation with the other two agents.

Warn them this is experimental — if the agents spin or get stuck, kill the session and start over with a tighter prompt.

After they finish, ask them what they noticed about peer-to-peer coordination vs the controller pattern from Exercise 2.
